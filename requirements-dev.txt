-r requirements.txt

# pytest 9 requires Python 3.10+, so 3.9 back-resolves to the 8.x line.
pytest>=8.0.0

# Static check that nothing raises the supported-Python floor above 3.9
# (see the version-floor job in .github/workflows/tests.yml).
vermin>=1.6.0
